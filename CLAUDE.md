# 주간 기술 스크리닝 루틴 — 도구 도입 정책

이 저장소에서 도는 주간 기술 스크리닝(스케줄 작업)이 "즉시 도입" 후보를 실제로
설치·안내할 때 지키는 규칙. 2026-08-31 작업에서 겪은 시행착오를 반영한다.

## 기본 제약

- 스크리닝을 도는 세션은 클라우드 컨테이너이며 사용자의 실제 PC(Windows 11)에
  직접 접근할 수 없다. 이 컨테이너 안에서 설치 명령을 실행해봐야 사용자
  컴퓨터에는 반영되지 않는다. **실제 설치는 항상 사용자가 자기 PC 로컬 세션에서
  명령을 직접 실행**해야 한다.

## 스코프 — 프로젝트에 묶지 말고 컴퓨터 전역으로

"즉시 도입" 후보는 특정 프로젝트 폴더에 묶지 않고 사용자 컴퓨터 전역(그 계정의
모든 프로젝트에서 공용)으로 설치되도록 안내한다. `.mcp.json`을 프로젝트에
커밋하거나 프로젝트 전용 스킬 디렉터리(`.claude/skills`, `.agents/skills`)에
설치하는 방식은 쓰지 않는다.

- **MCP 서버**: `claude mcp add --scope user <이름> -- <명령어>` 형태로 안내한다.
  `--scope`를 생략하면 기본값이 `local`(프로젝트+사용자 조합, 비공유)이라
  전역이 아니다.

  ```powershell
  claude mcp add --scope user <서버이름> -- npx -y <npm패키지>@latest
  ```

- **스킬** (`npx skills add`류 도구로 설치하는 것들): 반드시 `-g -a "*" -y`를
  함께 준다.

  ```powershell
  npx skills add <owner>/<repo> -g -a "*" -y
  ```

  이유: Claude Code 세션 밖(맨 PowerShell)에서 `-g`만 주면 실행 환경이 Claude
  Code를 자동 감지하지 못해 Codex·GitHub Copilot 등 다른 에이전트에만 연결되고
  Claude Code가 빠지는 경우가 있었다(2026-08-31 Archify 설치 사례로 확인).
  `-a "*"`로 명시해야 Claude Code를 포함한 모든 지원 에이전트에 확실히 연결된다.

## 설치 확인도 전역 플래그로

- `claude mcp list` (스코프 무관하게 전체가 뜬다)
- `npx skills list -g -a claude-code` (`-g` 없이 실행하면 기본이 프로젝트
  스코프라 cwd에 따라 결과가 달라진다 — 항상 `-g`를 붙인다)

## 문서 작성 시

주간 스크리닝 문서의 "로컬 설치 후보" 섹션도 위 형식(전역 스코프 명령 +
전역 확인 명령)으로 작성한다.
